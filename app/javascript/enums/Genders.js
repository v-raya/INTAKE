export const GENDERS_LEGACY = Object.freeze({
  M: 'Male',
  F: 'Female',
  U: 'Unknown',
})

const GENDERS = Object.freeze({
  male: 'Male',
  female: 'Female',
  intersex: 'Intersex',
  unknown: 'Unknown',
})

export default GENDERS
