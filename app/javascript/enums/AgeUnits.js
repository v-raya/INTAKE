// Use this enum if the API consists of Age Units in "Y", "M", "D" format.
// There are no WEEKS in this enum.

const AGE_UNITS = Object.freeze({
  Y: 'yrs',
  M: 'mos',
  D: 'dys',
})

export default AGE_UNITS
